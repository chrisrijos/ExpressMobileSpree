<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), class: 'p-image',:itemprop => "image" %>
<% else %>
  <%= product_image(@product, class: 'p-image', :itemprop => "image") %>
<% end %>
